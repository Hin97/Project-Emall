<% provide(:title, "Edit Trade") %>
<div class="row">
    <div class="col-md-10 col-md-offset-1">
    <section class="user_info">
      <h5>
      Purchase information
      </h5>
    </section>
    
      <ol class="items">
      <div class="row">
     <div class="col-md-4">
      <div style="background-color: #eaeaea; padding:20px;  border: 1px solid gray;border-radius: 25px;">        
      <div style="text-align: center;">
      <%= image_tag @trade.item.image.url if @trade.item.image? %>
      <br></br>
      <span class="name">Name: <%= @trade.item.name %></span>
      <span class="year">Year: <%= @trade.item.year %></span>
      <span class="condition">Condition: <%= @trade.item.condition %></span>
      </div>
      </div>
      </div>

      <div class="col-md-8">
      <div style="background-color: #eaeaea; padding:20px;  border: 1px solid gray;border-radius: 25px;">  
     <span class="price">Price: $<%= @trade.item.price %></span>
      <span class="category">Category: 
      <% @trade.item.category_ids.each do |id| %>
       "<%= Category.find(id).name %>"
      <% end %>
      </span>
      <span class="name">Seller: <%= link_to @trade.item.user.name, @trade.item.user %></span>
      <span class="name">Books in stock: <%= @trade.item.quantity%></span>
    <%= form_for(@trade) do |f| %>
      <%= render 'shared/error_messages', object: @trade %>
          <%= f.label "Quantity" %>:
          <br>
          <%= f.number_field :buyquantity, max: @trade.item.quantity, min: 1, style: "width:10%" %>
          <br>
        <%= f.hidden_field :user_id, :value => current_user.id %>
          <br></br>
     <%= f.submit "Pay", class: "btn btn-lg btn-success", style: "width:20%" %>
     <% end %>
      </div>
            <br></br>
     <%= link_to "Return to List", items_path, class: " btn btn-lg btn-success" %>
      </div>
      </div>
      </ol>
      </div>
</div>

